{
  "version": 2,
  "installCommand": "cd backend && npm install",
  "buildCommand": "echo 'No build step needed - serving static frontend'",
  "framework": null,
  "outputDirectory": "public",
  "env": {
    "NODE_ENV": "production"
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/backend/server.js" }
  ]
}
